## 1. Evidence and required Claude skills

- [x] 1.1 Record the supplied failure chain and distinguish observed log evidence from host-source diagnosis and pending validation.
- [x] 1.2 Prepare an application-owned OpenSpec plugin from official assets for managed Claude sessions with the required namespace in development and packaged builds.
- [x] 1.3 Wire registration into managed Claude invocation preparation while preserving model, effort, MCP, and existing plugin arguments; surface missing required assets before provider spawn.

## 2. Managed delegated work lifecycle

- [x] 2.1 Replace successful auto-finalization with bounded same-session foreground continuation while Claude reports outstanding delegated tasks.
- [x] 2.2 Preserve notification filtering, task completion updates, duplicate-result protection, cancellation, provider failures, and exactly-once turn usage across continuations.

## 3. Implementation verification and repair

- [x] 3.1 Require shared verification to prove requested implementation and acceptance criteria across all selected repositories as well as actual project checks.
- [x] 3.2 Make shared refinement recover missing or partial implementation independently of verdict labels while preserving existing work and explicit human blockers.
- [x] 3.3 Align factory and OpenSpec lifecycle decider goals with evidence of completion and a fresh PASS verdict.

## 4. Regression verification

- [x] 4.1 Run deterministic session tests for pending architect work, completion events, notification-only results, same-chunk updates, bounded deferral, Stop, and provider failures.
- [x] 4.2 Run asset and spawn-argument tests for namespace availability, development/package resolution, missing resources, existing plugins, and provider option preservation.
- [x] 4.3 Run command catalog and graph tests for clean baseline without implementation, cross-repository scope, repair routing, and conditional decider goals.
- [x] 4.4 Run the relevant integration and type checks, validate OpenSpec artifacts, and record final commands, results, and limitations in verification.md without a paid provider replay.
