## 1. Process lifecycle and output

- [x] 1.1 Implement execution identity, confirmed stop states, group/tree escalation and bounded shutdown drain.
- [x] 1.2 Capture bounded partial stdout/stderr, preserve terminal logs and expose truncation metadata.
- [x] 1.3 Validate lifecycle with regression tests and real isolated child processes.

## 2. API and mission operations

- [x] 2.1 Align REST/MCP lifecycle hooks, repository cwd validation, scoped listing, logs and idempotent stop responses.
- [x] 2.2 Update mission guidance and test ownership, reused identities, validation and failures.

## 3. Mission interface

- [x] 3.1 Reconcile process state across asynchronous stop, lifecycle events, reconnects and conversation switches.
- [x] 3.2 Add accessible chip inspection and searchable live log modal with pause/follow, filters and export.
- [x] 3.3 Translate all process controls and verify responsive UI and error states.

## 4. Verification

- [x] 4.1 Run affected regression suites, TypeScript, build and OpenSpec validation; document behavior and evidence.
