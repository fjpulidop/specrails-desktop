# D1b / D5 and native implementation checkpoint

Paused for the user's credit checkpoint on 2026-09-26. Source edits are stable;
the root agent owns shared-tree commits, PRs and final integration checks. Do not
claim the complete plan or D5 finished. No new features or expensive suites are
running from this subtask.

## Desktop D1b — implemented

- Core's schema is vendored byte-for-byte. `RuntimeConfig.roles` preserves the
  three historical `agents` entries and validates custom IDs, explicit source and
  artifact policies, optional skill, prompt, provider/model and escalation.
  Built-in descriptors cannot change their implicit policy.
- Project settings expose custom role creation/removal, prompt and independent
  permissions, reusing provider/model/effort/turn/escalation controls. New roles
  default to source read access and no artifact writes. All eight locales have
  the new labels. Project-switch/save ownership remains unchanged.
- The HTTP settings response advertises `openRolesAvailable`. Unsupported Core
  rejects custom-role saves before file writes. Default model resolution,
  connection-ID migration and launch overrides include custom roles and fixer.
- Capability validation accepts every declared role/engine selection, including
  escalation rows, with exact membership and duplicate rejection; no six-row cap.
- Narrow docs: `server/modules/agent-runtime/README.md`,
  `docs/internals/programmatic-agent-runtime.md`, and
  `desktop-role-factory-protocol.md` in this change.

Main source ownership: settings.ts, effective-config.ts, settings-router.ts,
settings schema, settings React components/lib, agentRuntime locale files; only
the capability-row validator functions in loader.ts (other loader changes belong
to the editor/runtime agent).

## Desktop D5 — four factories implemented; templates pending

`loop-core-factory.ts` defines capability-selected graphs for Implement, Batch,
Quick SDD and Freestyle. Factory IDs/aliases remain unchanged; old retained Core
receives legacy graphs. Every mutating successful exit requires verified delivery.
Batch uses isolated ticket implementations followed by global verification.
Quick SDD uses two native skill prompts, real validate/archive pieces and host
checks before and after archive. The editor/runtime agent wired capability-aware
selection into the factory gallery, detail and launch routes.

**Still pending:** port the eight named starters in `loop-templates.ts`:
`ship-and-green`, `verify-pass`, `ci-watch`, `lint-and-fix`, `type-safe`,
`coverage-climb`, `build-fix`, `deploy-check`. No template source was edited yet.
Preserve legacy graphs for older Core until D8; add capability-aware gallery and
clone selection. Watchers should have explicit read access. Preserve useful
prompts, goals and retry bounds; mutating templates need actual host verification,
and passing sentinels alone must never certify a candidate. `ship-and-green`
should reuse native implementation, then verify/decide/fix. D5's end-to-end
Desktop-bridge parity test and final pipeline user-guide review also remain.

## Core C6 fork completion added in this checkpoint

Core worktree: `/private/tmp/specrails-core-engine-v2`.

- `implementation-journal.ts` now captures the private archive-baseline marker
  with the immutable cut. Fork restore writes validated `sourceChange` provenance;
  recursive forks inherit it from checkpoint-owned bytes.
- `graph/artifacts.ts` normalizes only pinned OpenSpec's exact autogenerated
  Purpose placeholder for a newly created spec to that source change. It retains
  strict complete before/after byte comparisons and skips writes for an identical
  after-image. Authored Purpose and changed requirements remain real conflicts.
- `fork-archive.test.ts` exercises actual pinned CLI archives for identical replay,
  authored-Purpose conflict and requirement conflict, checking original spec
  bytes, inode and mtime. Binding tests cover recursive provenance.
- `pieces/README.md` and `pieces-protocol.md` clarify that unfinished native
  instances restore cut journals; completed parent instances inherit evidence
  without repeated provider calls. Source DB/journal/evidence stay unchanged;
  shared worktree edits may invalidate a fresh source inspection.
- The fork agent owns `engine/fork.ts` and the final real incomplete-fork retest;
  that result must be collected before claiming all C6 fork tests pass.

## Verification completed

- Desktop settings/custom-role client suites: **21 passed** (existing React act
  warnings in the project-switch test; no failures).
- Desktop settings HTTP suite after new save/rejection/migration coverage:
  **41 passed**, using local loopback permission.
- Earlier combined settings, open-role, effective-config, loader, factory suites:
  **109 passed**. This includes actual paired Core structural validation of all
  four factory definitions with Node 22.22.3; no pairing skip locally.
- Latest Desktop `npm run typecheck`: **passed**.
- Core fork archive + binding suites: **7 passed**; Core typecheck: **passed**.
- Core earlier native implementation suite: **3 passed**, including two-ticket
  Batch and approval replay; Quick SDD actual execution: **1 passed**; example
  definition validation: **passed**. Root owns the current full-suite result.
- Desktop `npm run audit:architecture`: **not yet green**; reports shared server
  dependency/public-subpath manifest drift. Review all agents' changes before
  regenerating `server/modules/boundaries.json`; rerun architecture and source-map
  generation once the shared edit set is frozen. Never lower boundary rules.

Local evidence logs: `/private/tmp/desktop-role-factory-tests.log`,
`/private/tmp/desktop-custom-roles-client.log`,
`/private/tmp/desktop-role-http-tests.log`,
`/private/tmp/desktop-open-roles-typecheck.log`,
`/private/tmp/desktop-role-architecture.log`,
`/private/tmp/core-fork-archive-tests.log`,
`/private/tmp/core-piece-typecheck.log`.

No new commit or PR was created from this shared worktree by this subtask.
Prior C2 is committed in PR #387; the root integrated its source into Core v2.
